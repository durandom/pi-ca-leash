List retained local subagent runs when you need run ids, drivers, states, or agent names.
- Use `subagent_list` to discover run ids before `subagent_status`.
- Treat attention notes as operator-facing diagnostics.
