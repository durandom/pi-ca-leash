Read older peer transcript history when you need to scroll back instead of asking the peer to repeat itself.
- Use `peer_history` for audit/debug context, not for polling live completion.
- Prefer small `limit` values.
