List current long-lived runtime-backed peers and their summarized activity.
- Use `peer_list` before asking or stopping a peer when you need current names and states.
- Do not poll repeatedly just to wait for completion; peer completions are relayed automatically.
