Assign a task to an existing teammate when you want retained worker state across tasks.
- Use `team_task` only after `team_spawn` or `team_list` confirms the teammate exists.
- Give a concise title and concrete details.
