Interrupt a peer that is busy or stuck. Prefer this before stopping/killing a peer.
- Use `peer_interrupt` only when the peer appears stuck, has gone down the wrong path, or the user redirects the task.
